#green
version: '3'
services:
  order-manage-server:
    build: .
    ports:
      - "8082:8080"
      - "8090:8090"
    container_name: order-manage-server-green

networks:
  default:
    name: order-manage-server-bridge
    external: true
